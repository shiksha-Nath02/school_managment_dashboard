# Production build config (read automatically by `vite build`).
# Relative path so the app works under HTTPS on any school's domain —
# nginx proxies /api to that school's backend. Not a secret; safe to commit.
VITE_API_URL=/api
